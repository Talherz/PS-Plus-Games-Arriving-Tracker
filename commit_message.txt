🔒 Fix ReDoS vulnerability in parseCatalogContent regex

Replaced the vulnerable headingRegex, which used non-greedy capturing ([\s\S]*?) leading to exponential backtracking on large or malformed inputs, with a simpler tag-matching regex combined with indexOf and substring. This eliminates the ReDoS vulnerability while keeping functionality intact.
